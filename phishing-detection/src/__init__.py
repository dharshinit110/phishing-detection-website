"""
Package initializer for the phishing detection source code.

This file allows `src` to be treated as a Python package so that
relative imports (e.g. `.feature_extraction`) work correctly when
running modules such as:

    python -m src.train_model
"""

